{
  "private": true,
  "scripts": {
    "build": "pwa export",
    "start": "sirv build -s",
    "watch": "pwa watch"
  },
  "dependencies": {
    "ganalytics": "^3.0.0",
    "sirv-cli": "^0.4.0"
  },
  "devDependencies": {
    "@pwa/cli": "latest",
    "@pwa/plugin-brotli": "latest",
    "@pwa/plugin-offline": "latest",
    "@pwa/preset-svelte": "latest",
    "svelte": "^3.1.0"
  },
  "browserslist": [
    ">0.25%",
    "last 1 version",
    "not ie_mob 11",
    "not ie 11",
    "not dead"
  ]
}